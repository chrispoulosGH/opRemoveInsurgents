# Tactical Command — Web Version

## Start

**Terminal 1 — Server:**
```
cd server
npm start
```

**Terminal 2 — Client:**
```
cd client
npm run dev
```

Open http://localhost:5173 in one or more browser tabs. Each tab is a separate agent.

## Scoring
`score = (10 × hits) + (25 − drones launched)`
